function void fee1(int i, j, k, x)
{
    function bool fee2(int l, m, n)
    {
	var bool x, j; /* declarations */
	var char k;
	k = '@'; /* statements */
	i = l + l;
	if ((k == '*') || (x != false) && (l + m < i)){ 
		x = l < m;
	}
        return x;
    }
    {
	var char x;
	var bool k;
	k = fee2(5,i,j);
     }
    x = k;
}

function void main(){}